import { StartsWithDrPipe } from './starts-with-dr.pipe';

describe('StartsWithDrPipe', () => {
  it('create an instance', () => {
    const pipe = new StartsWithDrPipe();
    expect(pipe).toBeTruthy();
  });
});
